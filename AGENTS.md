# AGENTS.md

## Canonical Buckets

- `agents/` contains agent workflow, shared skills, and Claude/Codex parity assets.
- `src/` contains backend, frontend, assets, and runtime code.


## Parity

Keep aligned with `CLAUDE.md`. Update `agents/workspace/shared/` first.